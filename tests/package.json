{
  "name": "wchs2026-tests",
  "version": "1.0.0",
  "private": true,
  "description": "Smoke tests for the generated WCHS 2026 schedule page (dev-only; the page itself has no dependencies)",
  "scripts": {
    "test": "node test.js"
  },
  "devDependencies": {
    "jsdom": "^29.1.1"
  }
}
